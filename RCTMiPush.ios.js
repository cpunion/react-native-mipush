/**
 * @providesModule RCTMiPush
 * @flow
 */
'use strict';

// var NativeRCTMiPush = require('NativeModules').RCTMiPush;

/**
 * High-level docs for the RCTMiPush iOS API can be written here.
 */

var RCTMiPush = {
  getInitialMessage: function(callback) {
    callback(null);
  }
};

module.exports = RCTMiPush;
